<div id="bigtable"></div>
<%--
<table>
<g:each var="row" in="${result.jsonOutput(chunk)}">
    <tr>
    <g:each var="item" in="${row}">
        <td>${item}</td>
    </g:each>
    </tr>
</g:each>
</table>
<a class="jscroll-next" href="/bi-prototype/job/${result.id}/chunk/${chunk + 1}">next</a>
--%>

